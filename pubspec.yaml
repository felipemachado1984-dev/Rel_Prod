name: relatorio_producao
description: App de captura de produção com OCR
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  google_mlkit_text_recognition: ^0.11.0
  image: ^4.1.7
  pdf: ^3.10.8
  path_provider: ^2.1.2
  share_plus: ^7.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
